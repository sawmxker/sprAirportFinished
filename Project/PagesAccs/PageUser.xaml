<Page x:Class="sprAirport.Project.PagesAccs.PageUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:sprAirport.Project.PagesAccs"
      mc:Ignorable="d"
      Title="PageUser"
      Background="#F0F2F5"
      Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MainBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.12"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Border Style="{StaticResource MainBorderStyle}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,10">
                    <TextBlock Text="Панель пользователя" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
                        <TextBlock Text="Откуда:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="tbFromCity" Width="140" Height="26" Margin="0,0,12,0"/>

                        <TextBlock Text="Куда:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="tbToCity" Width="140" Height="26" Margin="0,0,12,0"/>

                        <Button Content="Показать рейсы" Height="30" Width="140" Background="#EE88A6EE" Style="{StaticResource RoundedButtonStyle}" Click="BtnShowFlights_Click"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Id рейса:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="tbFlightId" Width="80" Height="26" Margin="0,0,8,0" ToolTip="Введите Id рейса из списка"/>
                        <TextBlock Text="Дата:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="dpFlightDate" Width="150" Height="26" Margin="0,0,12,0"/>
                        <Button Content="Проверить места" Height="30" Width="150" Background="#EE90A2CE" Style="{StaticResource RoundedButtonStyle}" Click="BtnCheckFlight_Click"/>
                    </StackPanel>
                </StackPanel>
                <Button x:Name="btnLogout"
                Content="Выйти"
                Width="80"
                Height="28"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="8,0,0,10"
                Background="#FF525961"
                Foreground="White"
                FontWeight="Bold"
                Click="BtnLogout_Click"
                DockPanel.Dock="Right"
                Style="{StaticResource RoundedButtonStyle}"/>

                <DataGrid x:Name="dgFlights" AutoGenerateColumns="True" DockPanel.Dock="Bottom" Margin="0,8,0,0" />
            </DockPanel>
        </Border>
    </Grid>
</Page>