<Page x:Class="sprAirport.Project.PagesAccs.PageAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:sprAirport.Project.PagesAccs"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="PageAdmin">

    <Page.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" >
            <Button Content="Управление самолетами"
                    Height="40"
                    Margin="0,0,0,10"
                    Background="#EE88A6EE"
                    Cursor="Hand"
                    Style="{StaticResource RoundedButtonStyle}"
                    Click="ManagePlanes_Click"/>

            <Button Content="Управление маршрутами"
                    Height="40"
                    Margin="0,0,0,10"
                    Background="#EE90A2CE"
                    Cursor="Hand"
                    Style="{StaticResource RoundedButtonStyle}"
                    Click="ManageRoutes_Click"/>

            <Button Content="Управление рейсами"
                    Height="40"
                    Margin="0,0,0,10"
                    Background="#EE88A6EE"
                    Cursor="Hand"
                    Style="{StaticResource RoundedButtonStyle}"
                    Click="ManageFlights_Click"/>

            <Button Content="Статистика и отчеты"
                    Height="40"
                    Margin="0,0,0,10"
                    Background="#EE90A2CE"
                    Cursor="Hand"
                    Style="{StaticResource RoundedButtonStyle}"
                    Click="ViewReports_Click"/>

            <Button Content="Управление пользователями"
                    Height="40"
                    Margin="0,0,0,10"
                    Background="#EE88A6EE"
                    Cursor="Hand"
                    Style="{StaticResource RoundedButtonStyle}"
                    Click="ManageUsers_Click"/>

            <Button Content="Выход"
                    Height="40"
                    Background="#FF525961"
                    Cursor="Hand"
                    Style="{StaticResource RoundedButtonStyle}"
                    Click="Logout_Click"/>
        </StackPanel>
    </Grid>
</Page>